# Methodology Document: Amazon Business Entity Resolution Challenge

**Team Name:** [Your Team Name]  
**Submission Date:** September 2026  
**Primary Metric (Macro F0.5):** Precision-Weighted Entity Resolution  

---

## 1. Executive Summary

This document describes the end-to-end machine learning solution for the Amazon Business Entity Resolution Challenge. The objective is to identify, for each Source 1 reference business entity, all matching records across Source 2 and Source 3. The solution is evaluated using macro-averaged per-Source-1 $F_{0.5}$, where precision carries twice the weight of recall, and true singletons (entities with no matches) are awarded a full 1.0 score when correctly predicted empty.

Our pipeline employs a **two-stage architecture**:
1. **High-Recall Multi-Route Candidate Generation (Blocking)**: Achieves $\ge 98\%$ candidate recall across 7 complementary blocking routes while reducing candidate space by $>99.9\%$.
2. **Calibrated GBDT Pairwise Matcher & Dedicated Singleton Gate**: Extracts 30+ similarity and discrepancy features, calibrates match probabilities, enforces learned cutoffs, and uses an entity-level singleton gate to eliminate false merges on singleton businesses.

---

## 2. Candidate Generation & Blocking Strategy

Candidate generation sets the theoretical recall ceiling for entity resolution. A match omitted at this stage cannot be recovered by downstream classifiers. We constructed a union of 7 independent blocking routes:

1. **Exact Core Name**: Normalizes entity names (lowercasing, punctuation stripping, Unicode NFKD accent folding, and stripping legal suffixes such as *Inc, LLC, Corp, Ltd, Pvt, SARL, SAS*).
2. **Alphabetically Sorted Core Tokens**: Inverts word-order permutations (e.g., *"Apex Healthcare Services"* $\leftrightarrow$ *"Healthcare Services Apex"*).
3. **Prefix / First-Token Inverted Index**: Matches on the first 5 characters of the primary content token (minimum length 4).
4. **Character $n$-gram TF-IDF Nearest Neighbors**: Extracts character 3–5 grams and queries sparse cosine nearest neighbors (resilient to spelling noise, typos, and modest transliteration).
5. **Rare-Token Overlap**: Inverted index matching tokens with high Inverse Document Frequency (IDF) to ensure distinctive brand names force candidate retrieval.
6. **Structural Address Match**: Dual-key index matching on exact 5/6-digit postal code and leading street number.
7. **Phonetic Hashing**: Soundex and Double Metaphone representation on primary tokens to catch phonetic spelling variations.

All candidate links generated by this union are saved directly to `output/candidate_pairs.tsv` as the exact candidate pool evaluated by the model.

---

## 3. Data Preprocessing & France Open-Set Generalization

The training dataset covers the US and India, whereas the test set introduces **France**. To ensure seamless zero-shot generalization to unseen French entities:
* **No Closed-Set Country Conditioning**: Country is never one-hot encoded or used as a hard filter.
* **Country Comparison Feature**: Encoded as an open-set comparison:
  * `+1` if both records share identical country text.
  * `0` if both records have conflicting country labels.
  * `-1` if country is missing or novel.
* **Accent Folding (`café` $\to$ `cafe`)**: Built into deterministic text normalizers to align accented and unaccented French entity names and street names.
* **Unified Legal Suffix Dictionary**: Normalizes French company designations (`SARL, SAS, SA, EURL, SCI, SNC`) and street types (`Rue, Av, Bd, All`) alongside US and Indian equivalents.

---

## 4. Feature Engineering

For each candidate pair `(Source 1, Candidate Source 2/3)`, we compute 30+ numerical signals across four families:

1. **Name Similarity Features**:
   * RapidFuzz `token_sort_ratio`, `token_set_ratio`, `levenshtein_ratio`, `jaro_winkler`, `partial_ratio`.
   * Character 3–5 gram Jaccard similarity and TF-IDF cosine similarity.
   * Core name exact equality and initials/acronym match flag.
   * Shared token IDF rarity (penalizes common generic words like *"Store"*, *"Market"*, *"Pharmacy"*).
2. **Address Similarity Features**:
   * Address token set ratio, address Levenshtein ratio, and address token Jaccard similarity.
   * Postal code exact match and postal prefix (3-digit) match.
   * Street number exact match and absolute integer difference $|num_1 - num_2|$.
3. **Discrepancy & Joint Evidence Features**:
   * Joint evidence: $\text{NameSim} \times \text{AddrSim}$ and $\min(\text{NameSim}, \text{AddrSim})$.
   * `Strong_Name_Weak_Addr`: High name match ($>0.85$) with low address match ($<0.25$) $\to$ isolates distinct chain branches.
   * `Weak_Name_Strong_Addr`: Low name match with identical address $\to$ isolates distinct businesses sharing a commercial complex.
4. **Context & Meta Features**:
   * Open-set country match flag (`+1, 0, -1`).
   * Target source indicators (`is_source_2`, `is_source_3`).
   * Candidate rank within the Source 1 candidate pool and candidate pool size.

---

## 5. Model Architecture & Singleton Gate

### 5.1 Calibrated GBDT Pairwise Matcher
* **Backbone**: Gradient Boosted Decision Tree (LightGBM / CatBoost) trained on pairwise similarity vectors.
* **Negative Sampling**: Trained on hard negatives surfaced from the blocking stage.
* **Probability Calibration**: Uses Platt scaling (logistic regression on logits) fitted on an unsampled out-of-fold validation set, ensuring outputs reflect true posterior probabilities.

### 5.2 The Dedicated Singleton Gate (Highest ROI for $F_{0.5}$)
Because singletons award 1.0 when empty and 0.0 upon any false positive, we train an entity-level classifier:
$$\text{Is\_Singleton}(S1_i) = f(\max(p), p_{\text{gap}}, \text{top3\_sum}, \text{cand\_count}, \text{name\_idf})$$
If the singleton gate probability exceeds a tuned cutoff, the entity is assigned an empty list `""`, shielding precision against marginal candidate false alarms.

### 5.3 Macro $F_{0.5}$ Threshold Sweeping
Decision boundaries are not fixed at 0.5. Cutoffs $\tau_{S2}, \tau_{S3}$ and $\tau_{\text{singleton}}$ are optimized via multi-dimensional grid search directly against the competition's macro-averaged $F_{0.5}$ metric on out-of-fold validation predictions.

---

## 6. Validation Strategy & Anti-Leakage Controls

* **Connected-Component Grouped 5-Fold Cross-Validation**:
  * An undirected graph is formed by linking Source 1 entities to their true target matches. Connected components are extracted.
  * Cross-validation folds are grouped strictly by Connected Component ID, guaranteeing that no entity or related records leak between training and validation.
* **Country Holdout Diagnostic**: Models trained on US records are evaluated on Indian records (and vice versa) to stress-test generalization and verify independence from memorized geographic vocabulary.

---

## 7. Submission Verification & Rule Compliance

* **Submission Verification**: Both output files are verified using `utils/validate_submission.py`.
  * Exactly one row per test Source 1 entity.
  * All matches are confirmed as a strict subset of candidate pairs: $\text{matched\_entity\_ids} \subseteq \text{candidate\_entity\_ids}$.
  * Singletons are formatted as empty strings `""` with no quotation or `NaN` artifacts.
* **Zero External Data**: No external lookups, geocoding APIs, web queries, or commercial ER services were used.
* **Licensing**: All algorithms and libraries used (Python standard library, NumPy, SciPy, Scikit-Learn, LightGBM, CatBoost, RapidFuzz) are MIT or Apache-2.0 licensed, operating with $\le 8\text{B}$ parameters.
